# URL base da API real (relatório de conversas, etc.)
WRAPPER_BASE_URL=http://localhost:3000

# Chave da API real (enviada no header X-API-Key em todas as chamadas)
GENERAL_REPORT_API_KEY=4fe0753506e3146329155e765adaaecf2c6091d404804cbd7a6c401062307e7d

# Pasta onde salvar JSON bruto e otimizado (por endpoint)
WRAPPER_CACHE_DIR=./cache

# Porta do servidor wrapper (python main.py)
WRAPPER_PORT=8000

# Opcional: para não commitar agentId, em config/api_endpoints.json use
# "agentId": "${WRAPPER_REPORT_LIA_AGENT_ID}" e defina abaixo:
# WRAPPER_REPORT_LIA_AGENT_ID=68fbc1cecf793a2cbf2159ab
